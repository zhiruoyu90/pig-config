zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000
  routes:
    auth-service:
      path: /auth/**
      serviceId: pig-service-auth
      stripPrefix: false
      sensitiveHeaders:

server:
  port: 1000
