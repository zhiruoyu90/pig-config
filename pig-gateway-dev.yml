zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000
  routes:
    auth-service:
      path: /oauth/**
      serviceId: pig-service-auth
      stripPrefix: false
      sensitiveHeaders:

server:
  port: 1000
